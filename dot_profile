# Sourced by login shells, not necessary, but can come in handy to just `source ~/.profile` from whichever current shell

if [ -n "$BASH_VERSION" ]; then
    [ -f "$HOME/.bashrc" ] && source "$HOME/.bashrc"
elif [ -n "$ZSH_VERSION" ]; then
    [ -f "$HOME/.zshrc" ] && source "$HOME/.zshrc"
fi
