#!/usr/bin/env zsh

set -eu

# hash: {{ include "../resources/Brewfile.tmpl" | sha256sum }}
export HOMEBREW_NO_ENV_HINTS=1
BREW_CONTENT=$(chezmoi execute-template < "$CHEZMOI_WORKING_TREE/resources/Brewfile.tmpl")
echo "$BREW_CONTENT" | brew bundle install --file /dev/stdin
