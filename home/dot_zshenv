#!/bin/zsh
# sourced by all zsh runs (interactive and non-interactive); keep this minimal
# debug with: env -i HOME="$HOME" PATH="/usr/bin:/bin" zsh -xc 'echo "PATH=$PATH"'

[ -r "$HOME/.config/shell-path.sh" ] && . "$HOME/.config/shell-path.sh"
