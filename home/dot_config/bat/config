# See `bat --list-themes`, "DarkNeon" is not great for man pages, good otherwise
--theme="Monokai Extended"  # Not great for .md, good otherwise

# See `bat -L`
--map-syntax ".*ignore*:Git Ignore"

--style=numbers,changes,snip
--wrap=never
